
· NPMRC
# Blokir preinstall/postinstall/install script bawaan paket demi mitigasi
# serangan supply-chain npm (mis. worm yang menyebar lewat postinstall).
# Paket yang MEMANG butuh script (better-sqlite3) di-build manual terpisah
# setelah install — lihat Dockerfile / README.
ignore-scripts=true